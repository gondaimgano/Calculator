package zw.co.calculator.repos

import javax.inject.Inject

class OperationRepositoryImpl
@Inject
    constructor():OperationRepository {
    override fun update(x: String): String {
        return ""
    }
}